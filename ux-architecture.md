# PromptHub UX Architecture

> 状态：历史 UX 参考。采集、同步、可编辑性和已废止功能以 [OPERATING_RULES.md](OPERATING_RULES.md) 为准；本文中关于 `Try / 去生成` 的历史描述已不再生效。

## 目标

把 BananaPrompts 详情页的核心交互迁移到 PromptHub：用户从探索流进入提示词详情后，可以看图、读结构化提示词、复制、去生成、收藏、点标签继续探索，并能回到来时的上下文。

## 当前用户任务闭环

1. 发现：从首页精选、分类卡、探索页、收藏页进入提示词。
2. 判断：在详情页查看结果图、参考图、分类、宽高比、模型、热度和结构化章节。
3. 使用：点击“一键复制提示词”，或点击“Try / 去生成”进入来源生成页。
4. 沉淀：收藏或删除收藏。
5. 再探索：点击标签、分类或相关提示词继续浏览。
6. 返回：从详情页返回首页、探索筛选结果或我的收藏，不丢失上下文。

## 信息架构

- 全局导航层：首页、探索提示词、导入、我的收藏、全局搜索。
- 浏览发现层：首页精选、探索网格、分类结果、标签结果、关键词结果。
- 提示词详情层：图片参考、任务动作、结构化 Prompt、元数据、标签和相关推荐。

## 本轮修复的断点

- 详情页返回固定回探索页，已改为返回来时上下文。
- 分类和标签筛选没有稳定 URL，已新增 `#/category/<name>`、`#/tag/<name>`、`#/explore?...`。
- 详情页缺少 BananaPrompts 的 Try 动作，已根据 BananaPrompts slug 生成来源创建链接。
- 目标页的 Reference Images 没有映射到 PromptHub，已补充参考图展示。
- 探索页筛选状态不够可见，已新增当前筛选条和清空动作。

## 设计原则

- 详情页是任务页，不只是展示页；复制和 Try 必须在标题区一眼可见。
- 任何筛选状态都应该可见、可清空、可分享、可刷新恢复。
- 用户从哪里进入详情，就应该能回到哪里。
- 相关提示词和标签都服务于继续探索，不把用户带进死胡同。
- PromptHub 保持自己的中文信息结构和品牌样式，只吸收 BananaPrompts 的交互骨架。

## 验证限制

BananaPrompts 在当前环境中可读取 DOM 内容，但截图接口连续超时，因此本轮以 DOM 结构和可见交互文本作为参考证据，不能声明像素级视觉还原。
