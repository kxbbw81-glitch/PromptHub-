# PromptHub 运行规则

**状态：生效**  
**最后整理：2026-07-29**  
**单一规则源：本文件**

本文件把此前分散在聊天记录、插件说明和实现中的约定收敛为可执行规则。聊天记录只作为需求来源；一旦某项决定写入本文件，后续实现、测试和说明均以本文件为准。

## 1. 规则优先级与变更方式

1. 用户最新的明确指令优先，并且必须在本文件的“变更记录”中写明它替换的旧规则。
2. 本文件优先于旧聊天记录、旧说明和旧截图。
3. 已上线代码描述当前行为；代码与本文件不一致时，先修复不一致，再宣称功能完成。
4. 涉及采集、去重、同步、数据结构或状态文案的改动，必须同时更新本文件与对应自动测试。
5. 视觉和页面布局规则放在 `ux-architecture.md`、`design-qa.md`；这里保留与数据、流程和用户承诺相关的产品约束。

## 2. 数据归属

- `data/collections.json` 是收藏数据唯一的权威来源，位于 GitHub `main` 分支。
- GitHub 主站 `https://kxbbw81-glitch.github.io/PromptHub-/` 从该数据源展示收藏。
- 国内站 `https://prompthub.kxbbw81.workers.dev/` 只展示已完成国内发布的数据。
- 国内站读取随部署发布的本地 `/data/collections.json`，不得在用户浏览器中依赖 `raw.githubusercontent.com` 才能显示收藏数据。
- 浏览器扩展的 `chrome.storage.local` 仅可保存短暂的失败重试队列和本机 Token；它不是收藏数据库，也不能成为跨设备数据来源。
- 新收藏必须按 `githubSyncedAt` / `collectedAt` 倒序展示，最新的排在最前面。

## 3. 收藏准入与识别

收藏一项提示词前，系统必须完成以下检查：

1. 提取完整的提示词正文，而不是界面标题、模型名称、广告文案或诸如 “GPT Image 2 on ChatGPT” 的上下文标签。
2. X 内容必须先展开完整帖子和同线程中的实际提示词，再进行识别。
3. 提示词必须达到当前完整度阈值（不少于 160 个字符），并具有实际图像、视频封面或可定位的结果媒体。
4. 必须保存可追溯的原始帖子 URL。X 的来源 URL 必须是具体状态页，不能使用 `/i/bookmarks`、首页或列表页。
5. 同时保存标题、提示词、媒体、宽高比、来源、分类、标签和采集时间；系统自动推断的字段允许在详情页编辑，回车保存。
6. 视频同样可以收藏，但至少要保存原帖 URL、封面/媒体和完整提示词。
7. 自动分类以内容用途优先：明确属于电商的内容归入“电商视觉”；其他 `mediaType` 为 `video` 的内容归入“视频提示词”。其余内容按标题、标签和完整提示词的加权主题规则归入统一中文分类，不能直接信任外部来源提供的英文分类。
8. 电商内容采用两层结构：一级分类为“电商视觉”，二级用途为产品主图、场景种草、广告海报、商品详情页、模特展示、UGC / 口碑、品牌视觉或电商视频。`mediaType` 仍保留为独立媒体属性，因此电商视频也必须能通过“视频提示词”筛选找到。

不满足上述条件的内容不得写入 GitHub 主站，并应返回明确失败原因。

## 4. 唯一性规则

同一收藏不得重复进入主站。按以下顺序判定：

1. 相同规范化来源帖子 URL，视为同一项。
2. 不同来源但规范化后的完整提示词相同，视为同一项。
3. 同一项再次采集时不新增记录；可以补全已有记录中缺失的媒体、标题、宽高比等字段。

去重必须在写入 GitHub 前和并发队列合并时执行。不得以“插件本地没有重复”代替主站去重。

## 5. 固定同步状态机

```text
识别与校验 -> 本机短暂队列 -> 写入 GitHub 主站 -> GitHub 确认
                                            |
                                            +-> 30 分钟后由 GitHub Action 发布到国内站
```

- 收藏动作先进入本机短暂队列，以防多个同时收藏请求相互覆盖。
- 队列立即自动写入 GitHub 主站；收到 GitHub 成功确认后才从队列移除。
- GitHub 写入失败时保留队列并重试，向用户显示失败原因；不能把“已进入本机队列”称为“已同步”。
- 国内站发布以 `githubSyncedAt + 30 分钟` 为准，由 GitHub Action 定时任务执行。扩展和页面不得抢先发布，也无需在正常情况下提示这一步。
- 国内站只在发布失败时提示异常；正常延迟发布不打扰用户。

## 6. 面向用户的状态文案

- GitHub 成功：`已写入 GitHub 主站 X 个提示词`。
- 每小时公开采集实际写入成功：主动提醒 `本次已同步 GitHub 主站 X 个提示词`；仅在 X 大于 0 时提醒。
- 没有新增：`GitHub 主站无新增，X 个提示词已存在`。
- 失败待重试：`待同步 X 个提示词`，并显示 GitHub 写入失败原因。
- 禁止单独使用“已同步”表示成功，除非同时说明成功目标站点。
- “待同步”只表示真实未完成的失败队列，不表示已在 GitHub 主站的数据。

## 7. Grok 公开检索采集范围

- 禁止使用浏览器自动操作 X 的书签页、首页、搜索页或任何登录态页面；旧的每小时书签/首页扫描任务已暂停并废止。
- 公开内容发现仅允许通过本机已登录的官方 Grok CLI 使用其托管 X Search 能力完成，不使用 X API，也不传入 X Cookie、书签或首页数据。
- 检索规则由 `config/grok-x-discovery.json` 维护：只以文生图、文生视频、完整 Prompt 等关键词和识别信号寻找公开帖子，不以个人时间线为输入。
- `config/grok-x-creators.json` 是重点创作者库。每轮公开采集先按优先级轮换扫描其中的创作者账号，再用通用关键词补充候选；创作者名单不构成任何校验豁免。
- Grok 候选批次必须遵循 `GROK_CANDIDATE_FORMAT.md`，写入本机 `staging/grok-x-cli-output.json`；候选文件不包含或声明 `githubSyncedAt`、`domesticSyncedAt`。
- Grok 只能输出候选 JSON。候选必须经过 PromptHub 的完整提示词、结果媒体、具体 X 状态页 URL、唯一性与安全校验后，才可以写入 GitHub 主站。
- 自动采集与手动插件采集共用同一份去重、校验和同步状态机；Grok 不得直接提交 GitHub，也不得绕过测试。
- 默认不启用定时运行。完成小批量无写入验证并由用户确认后，才可启用受限频率的公开检索任务。

## 8. 页面与可编辑性承诺

- 每条提示词有独立详情页，包含标题、结果媒体、完整提示词、分类、标签、模型、宽高比和来源信息。
- 系统自动识别的标题、提示词、链接、图片和元数据均可编辑；回车保存到主站数据源。
- 首页瀑布流、探索页、分类页和详情页均以主站数据源为准；分类数量随收藏数据实时重新统计。
- 国内站的页面与收藏数据请求应允许缓存和重新验证；结果媒体使用异步、延迟加载，外部图片失效或变慢不能阻塞文字与页面结构出现。
- 首页主文案为：`探索高品质纳米提示词库。` 不使用 “Nano Banana” 品牌文案。
- 未经明确需求，不新增 “Try / 去生成” 独立页面或操作入口。

## 9. 插件更新边界

以下情况**需要**重新打包并让用户更新浏览器插件：扩展的识别逻辑、权限、弹窗、收藏交互、同步协议或扩展内状态文案发生变化。

以下情况**不需要**更新插件：主站页面样式、详情页编辑能力、首页瀑布流、SEO 页面、GitHub 数据内容、国内站发布脚本或服务端同步规则发生变化。

## 10. 已废止规则

| 旧规则或说法 | 状态 | 替代规则 |
| --- | --- | --- |
| “手动点击同步到网站”才会写入主站 | 已废止 | 收藏后自动写入 GitHub 主站。 |
| GitHub Pages `localStorage` 是收藏中转或数据源 | 已废止 | `data/collections.json` 是唯一权威来源。 |
| `/i/bookmarks` 可作为收藏来源链接 | 已废止 | 必须保存具体帖子状态 URL。 |
| Codex 每小时浏览器扫描 X 书签页和首页 | 已废止 | 仅可通过官方 Grok CLI 发现公开、关键词命中的候选内容，且默认不定时运行。 |
| 本机队列可称为“已同步” | 已废止 | 只有 GitHub 成功确认才是主站同步成功。 |
| 扩展负责计时并发布国内站 | 已废止 | GitHub Action 在主站成功 30 分钟后发布国内站。 |
| 详情页必须提供 “Try / 去生成” | 已废止 | 不显示该未请求功能。 |

## 11. 变更记录

| 日期 | 决定 | 替换内容 |
| --- | --- | --- |
| 2026-07-29 | 建立本规则文件作为单一规则源。 | 分散的聊天式约定和过期插件说明。 |
| 2026-07-29 | 收藏后立即写 GitHub 主站，国内站延迟 30 分钟发布。 | 旧的手动同步流程。 |
| 2026-07-29 | 本机存储只用于短暂队列与失败重试。 | 把本机存储作为收藏数据库的做法。 |
| 2026-07-29 | 停止 X 浏览器书签/首页扫描，改为 Grok CLI 的公开关键词候选发现。 | 旧的每小时浏览器扫描规则；不使用 X API。 |
| 2026-07-30 | 视频统一归入“视频提示词”，自动采集采用本地加权分类规则。 | 直接保存外部候选分类名称的做法。 |
| 2026-07-30 | 增加“电商视觉”一级分类和二级用途筛选。 | 将电商用途拆成大量首页主分类的做法。 |
